# Plans · Ecosystem · Projects · Clients — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans (inline). Steps `- [ ]`.

**Goal:** Four content sections (overviews + concise detail pages) + the clients wall, data-driven, reusing Breadcrumb + breadcrumbLd.

**Architecture:** New `data/concerns.ts`; extend `data/plans.ts` + `data/projects.ts`; build overview + `[slug]` routes for plans/ecosystem/projects and a `/clients` wall. All prerendered (`generateStaticParams`, `dynamicParams=false`).

## Global Constraints

- Concise, defensible, in-brand copy. NAP/founder from `siteConfig`.
- Reuse primitives, `Reveal`, hue `Eyebrow`, `Tag`, `Breadcrumb`, `breadcrumbLd`.
- Spectrum accent-only; reduced-motion safe; per-page metadata; BreadcrumbList JSON-LD on detail pages.
- Verify per task: `type-check`+`lint`+`build` clean. Commit per task (branch `feat/sections`).
- Next 16: dynamic `params` is a Promise — `await params`.

## Tasks

### Task 1: Data — concerns + plans + projects
- [ ] Create `data/concerns.ts`: `Concern = { slug, name, category, description }`; `concerns: Concern[]` (14, slugs matching existing `/ecosystem/<slug>` hrefs); `getConcern(slug)`.
- [ ] Update `data/navigation.ts`: derive `sisterConcerns` from `concerns` (`concerns.map(c => ({ label: c.name, href: '/ecosystem/'+c.slug }))`). Keep other exports.
- [ ] Extend `data/plans.ts`: add `overview`, `context`, `support: string[]`, `ctaLabel`; add `getPlan(slug)`.
- [ ] Extend `data/projects.ts`: add `overview`, `description`, `highlights: string[]`; add `getProject(slug)`.
- [ ] type-check. Commit.

### Task 2: Plans overview + detail
- [ ] `app/plans/page.tsx`: metadata; ladder overview (rung №, name, audience, promise, link) + CTA.
- [ ] `app/plans/[slug]/page.tsx`: `generateStaticParams`, `dynamicParams=false`, `generateMetadata` (await params); Breadcrumb + hero (rung №, name, audience, promise) + context + support list + CTA (`ctaLabel` → /contact) + prev/next; BreadcrumbList JSON-LD.
- [ ] type-check + lint + build. Commit.

### Task 3: Ecosystem overview + detail
- [ ] `app/ecosystem/page.tsx`: metadata; intro + concerns grouped by `category` (grid of links) + CTA.
- [ ] `app/ecosystem/[slug]/page.tsx`: static params/metadata; Breadcrumb + hero (name + category eyebrow) + description + "Back to ecosystem" + Contact CTA; BreadcrumbList JSON-LD.
- [ ] type-check + lint + build. Commit.

### Task 4: Projects overview + detail
- [ ] `app/projects/page.tsx`: metadata; editorial cards (name, sector/status tags, location, overview) linking to detail + CTA.
- [ ] `app/projects/[slug]/page.tsx`: static params/metadata; Breadcrumb + hero (name + Tag sector/location/status) + description + highlights list + CTA + prev/next; BreadcrumbList JSON-LD.
- [ ] type-check + lint + build. Commit.

### Task 5: Clients wall
- [ ] `app/clients/page.tsx`: metadata; hero + trust line; grouped monochrome logo wall reusing `clients` (filter `featured && !holdForPermission`) via `next/image` fill + object-contain (same pattern as proof-bar); CTA. No detail pages.
- [ ] type-check + lint + build. Commit.

### Task 6: Verify
- [ ] `npm run dev`; confirm all 4 overviews + sample detail in each section render; breadcrumb JSON-LD present; homepage links to `/plans/*`, `/ecosystem/*`, `/projects/*` resolve (no 404); dev log clean. Commit fixes.

## Self-Review
**Spec coverage:** concerns/plans/projects data (T1), plans (T2), ecosystem (T3), projects (T4), clients (T5), verify (T6). ✓
**Types:** `Concern/getConcern`, `getPlan`, `getProject` (T1) used T2–T5; `sisterConcerns` derived from concerns keeps footer/teaser working. ✓
**Links:** every `/plans/<slug>`, `/ecosystem/<slug>`, `/projects/<slug>` referenced by Phase 3/4 now has a route. ✓
